# Local demonstration operations

Owner: the person running this example. Environment: disposable local process.

A nonzero exit code signals rejected input. The program performs no network requests and keeps no data after exit. To recover, correct the input and run again.

Inspect the source and run invalid input. Record the actual result in a separate observation file. No scheduler, deployment, or production monitoring is created.
