# Local demonstration release

Candidate: the files in this disposable example project. Target: a local Python process. No hosting or publication is included.

Run the acceptance command. Run app.py with two identical valid URLs and one invalid URL in a separate invocation. Confirm one stored item and an error for the invalid input.

Stop if a check fails. Recovery is to discard this disposable copy. No production data exists. Record the actual output in a separate observation file before recording the manual release check.
