# Installation journey

Builder need: distinguish valid source, a stale copied plugin and actual loaded behavior. Start with one read-only diagnostic command. Failures name the resource or mismatch; successful file checks show host_loading unverified. The host chooses its own installation flow. A new chat checks skill selection and bundled paths. The portable output is JSON for agents; the guide explains the user's next action. Human usability, accessibility of host UI and time-to-first-task remain unmeasured.
